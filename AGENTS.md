# 项目维护约定

- 本机工作根目录为 `E:\AI\daily-global-briefing\`。本任务产生的文件、临时文件、工具缓存、诊断下载和日志都放在该目录内；不要在其他目录另建副本。按工具实际支持的方式设置缓存目录，不能假定设置一个环境变量就覆盖所有缓存。应用自身的对话存储不属于本项目可控制范围。
- 开始工作先检查本仓库与 `.private/deployment/` 的状态和远端。公开仓库保存通用实现；正式个人运行位于私有部署仓库。
- 项目变化和实际错误及时追加到项目内日志。文件监听日志记录路径变化；人工日志还需写清原因、验证结果、未解决问题和远端同步结果。不得把调查推断写成已复现事实。
- 用户已于 2026-09-23 授权以后相关工作及时推送到 GitHub。每个可审查的修改批次完成适当验证后，当次提交并推送到对应仓库，再核验远端提交；不必重复请求已经给出的同步授权。授权不包含不相关的工作流派发或新邮件发送。
- 私人需求、任务记录和内部分析同步至 `.private/deployment/docs/private/`。通用代码和公开维护说明同步至公开仓库。凭据、原始抓取缓存、完整邮件和原始运行诊断不进入源码提交；需要保留时存于项目内的忽略目录。
- 不使用 `git add .` 或后台自动提交全部文件。按文件明确暂存，检查提交差异，发现推送失败立即记录并如实报告。
- 用户要求先浏览或讨论时，先提交调查结论和可比较样稿；建议中的内容范围、栏目形式和篇幅不自动成为已确认需求。确定后再改正式生成行为。
