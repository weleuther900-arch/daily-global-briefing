# 安全政策

## 支持范围

当前 `main` 分支是受支持版本。安全修复会优先处理可能导致凭据泄露、未经授权的邮件投递、来源访问边界绕过或不可信网页指令影响运行流程的问题。

## 报告漏洞

请不要在公开 Issue、讨论区、Pull Request 或日志中粘贴密钥、访问令牌、真实邮箱或可利用细节。

优先使用 GitHub 的私密漏洞报告功能（若仓库已启用）。若该功能不可用，请通过与维护者既有的私密联系渠道请求安全报告方式，只提供足以建立联系的最少信息。

报告应包含受影响版本、复现条件、影响范围和安全修复建议；请删除或替换所有敏感值。

## 凭据处理

- 仅将密钥和投递地址保存为 GitHub Actions Secrets 或本机受保护环境变量。
- 不要将 `.env`、运行状态、缓存、邮件文件或屏幕截图提交到仓库。
- 怀疑密钥泄露时，应先在相应服务商处撤销或轮换密钥，再删除受影响的 Git 历史或日志。
