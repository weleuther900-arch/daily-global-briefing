# 贡献指南

感谢你考虑参与 Daily Global Briefing。项目欢迎改进来源稳定性、筛选规则、邮件阅读体验、测试和文档。

## 开始前

1. 阅读 [README](README.md)、[来源与覆盖](docs/来源与覆盖.md) 和 [部署指南](docs/部署指南.md)。
2. 先搜索现有 Issue，避免重复工作；功能较大时先说明目标、数据来源和预期行为。
3. 不要提交任何密钥、真实邮箱、令牌、抓取缓存、邮件正文或个人运行记录。

## 本地验证

项目使用 Node.js 20 或更高版本，当前不需要安装第三方运行时依赖。

```powershell
npm test
npm run build:sample
npm run run:sample
```

涉及来源发现、正文提取或路由规则的修改，应补充或更新相应的 `tests/*.test.cjs`。样例必须使用虚构或可公开复用的内容，不能作为真实新闻发布。

## 来源贡献原则

来源配置位于 `config/sources.v1.json`。新增来源时，请同时说明：

- 该来源填补了哪个主题或覆盖组的缺口；
- 为什么它适合用作原始事实依据、发现入口或交叉核对来源；
- 入口 URL、HTTPS 域名白名单、解析方式和合理的单次条目上限；
- 公开访问、发布时间和页面稳定性的验证结果。

来源发现只用于形成候选，不代表自动刊登。不得加入绕过付费墙、访问控制或 robots 限制的抓取方式。

## 提交要求

- 保持 CommonJS 与现有代码风格，优先小而可验证的改动。
- 在提交说明中写清行为变化、测试覆盖和配置迁移需求。
- 任何影响邮件投递、成本、默认时间表或安全边界的改动，必须同步更新 README 或部署文档。
- 提交 Pull Request 前运行 `npm test`，并确认 `git status` 没有缓存、输出或密钥文件。

安全漏洞请不要通过公开 Issue 报告，详见 [安全政策](SECURITY.md)。
