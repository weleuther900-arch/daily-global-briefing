# 维护日志

## 2026-09-23

- 增加根目录 `AGENTS.md`，记录项目内存储、实时日志、按批次 GitHub 同步以及公开和私人资料的维护约定。
- 为 `.diagnostics/` 补充 Git 忽略规则，使既有诊断目录与公开部署文档中的私密产物约定一致。
- 对照历史需求、正式部署源码和实际审计产物检查内容覆盖与商业思考。私人反馈、运行数据、证据和讨论样稿保存到私人仓库资料区。
- 本批只修改维护规则、忽略规则和文档；未修改采集、模型生成、调度或邮件发送行为。验证使用差异、文档引用及暂存文件检查。

- 验证完成：公开项目自动化测试 95/95 通过，差异检查通过，诊断目录忽略规则生效。

- 验证：公开测试 95/95 通过，差异检查通过，诊断目录忽略规则生效。暂存曾因 Git 元数据写权限受限而受阻，后续同步以实际远端核验为准。

- 换机恢复步骤统一为公开仓库根目录 `E:\AI\daily-global-briefing\` 与私有部署目录 `.private/deployment\` 的嵌套结构，并加入两个远端与两套本地测试的核验命令。
- 本次维护复核中，公开仓库 118/118、私有部署仓库 120/120 自动化测试通过；未调用模型、未发送邮件。沙箱禁止创建公开仓库的 `.git/FETCH_HEAD`，因此以远端只读引用核验当前 `main`，不把失败的 fetch 记为同步成功。
- 已推送并核验本批公开提交 `6d939d97e5ab0378e048aa2d4d2c1c86eb0edf36`；GitHub `origin/main` 返回相同 SHA。对应私有部署提交 `67ff7f6ba4f11e731eb9fe991746bfdb75e0863d` 也已推送并核验。未派发工作流、未调用模型、未发送邮件。
